import { EntidadDetalleVenta } from './entidad-detalle-venta';

describe('EntidadDetalleVenta', () => {
  it('should create an instance', () => {
    expect(new EntidadDetalleVenta()).toBeTruthy();
  });
});
